# 本地/Agent 私有（不入库）
.artifacts/
.agent/
.agent_artifacts/
.test_tools/
.cursor/
.audit/
open/
start
stop
HANDOFF.md

# 本机分析、取证、进度板、会话产物、说明文档（本地可留，不入库）
analysis/
docs/

# fixtures：临时证据与探针（仅保留非 _ 前缀共享夹具）
fixtures/_*
fixtures/_*/
fixtures/*.runtime.json
fixtures/_devkit/

# 本地/Agent 辅助脚本（CI 只用 tools/ci 与 tools/repack）
tools/xiangse_devkit.py
tools/ios_mcp_client.py
tools/frida/
tools/frida_inspect.py
tools/reader-forensics/

# 构建产物
dist/
dist-ci/
dist-ci-*/
artifacts/
__pycache__/

# Xcode / SPM
*.xcuserstate
DerivedData/
.build/
LegadoBridge/.build/

# 默认排除 IPA；仅放行 CI 基线包（禁止 hook103 / 其它旁路包入库）
*.ipa
!ipa/.gitkeep
!ipa/香色闺阁2.56.1_未加密.ipa
*hook103*
ipa/*hook103*
