---
description: 真机 MCP 收工必须 press_power 熄屏（防会话压缩遗忘）
alwaysApply: true
---

# 真机收工熄屏（硬规则）

会话摘要/压缩**不能**当作已记住本条的理由。本文件 `alwaysApply: true`，每轮自动注入。

## 必须做

凡本回合对真机 MCP（默认 `http://192.168.1.18:8090`）做过任一操作（装包、启停、点击、截图、OCR、读沙盒、深链等），在以下时机的**最后一步**必须调用：

```text
press_power
```

触发时机（任一即触发，且只做一次短按电源）：

1. 本回合任务收工 / 准备结束回复
2. 用户说睡觉、先停、今晚到这、不用真机了
3. 长时间不再继续真机操作（去改代码/等 CI 且本回合不再碰设备）

## 禁止

- 以「摘要里没写」「换会话」「压缩后忘了」为由跳过熄屏
- 收工回复里只写「记得熄屏」却不实际调用 `press_power`
- 把 `press_home` 当成熄屏

## 唤醒

下次真机活开始：先 `wake_and_home`，再用 `screenshot` / `get_frontmost_app` 确认已醒。
