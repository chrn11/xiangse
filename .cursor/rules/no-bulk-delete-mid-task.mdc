---
description: 任务执行中禁止批量删除文件（防 hook 卡住）
alwaysApply: true
---

# 禁止执行中批量删文件

## 必须遵守

任务执行过程中**非必要不要批量删除文件**。批量删会触发 Cursor hook 卡住。

## 允许

- 单文件删除（明确路径）
- 用户明确批准后的批量清理
- 用户醒来后统一批准再删

## 禁止（执行中）

- `Remove-Item -Recurse` 大批目录
- 批量删 `analysis/`、`dist/`、`fixtures/`、解包产物等
- `git clean -fd` 等大范围清理（除非用户当场明确要求）

不确定时：跳过删除，记到待办，等用户批准。
